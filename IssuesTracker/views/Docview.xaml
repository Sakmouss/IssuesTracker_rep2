<UserControl x:Class="IssuesTracker.views.Docview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IssuesTracker.views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">
    <StackPanel  >
        <Button HorizontalAlignment="Right" Margin="10"  >X</Button>
        <StackPanel Height="577" Width="780" Margin="10">
            <ContentControl x:Name="docarea" Width="780" Height="578"></ContentControl>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Height="42" Margin="10">
            <Button x:Name="btnPrint" Margin="550,0,0,0" Width="100">Print</Button>
            <Button x:Name="btnSave" Margin="20,0" Width="100" >Save</Button>
        </StackPanel>

    </StackPanel>
</UserControl>
